# .github/workflows/clean-news-json.yml
name: Clean news.json

# Permessi espliciti per consentire il push del workflow
dependabot:
permissions:
  contents: write

on:
  push:
    paths:
      - news.json
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  clean:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true  # usa il GITHUB_TOKEN per le operazioni Git

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Clean HTML/CSS remnants from body fields
        run: |
          jq '
            def clean:
              if type == "string" then
                # Rimuovi blocchi <style>…</style> e <script>…</script>
                gsub("<style[\\s\\S]*?</style>"; "") |
                gsub("<script[\\s\\S]*?</script>"; "") |
                # Rimuovi tutti i tag HTML
                gsub("<[^>]+>"; "") |
                # Rimuovi commenti HTML
                gsub("<!--[^>]*-->"; "") |
                # Rimuovi blocchi CSS residui (es. .class{...})
                gsub("\\.?[A-Za-z0-9_\\-]+\\s*\\{[^}]*\\}"; "") |
                # Rimuovi proprietà CSS con valori (es. color:#fff;)
                gsub("[A-Za-z\\-]+:[^;]+;"; "") |
                # Decodifica &nbsp; e rimuovi altre entità
                gsub("&nbsp;"; " ") |
                gsub("&[a-zA-Z0-9#]+;"; "") |
                # Collassa whitespace multipli
                gsub("\\s+"; " ") |
                # Trim spazi esterni
                sub("^\\s+"; "") |
                sub("\\s+$"; "")
              else . end;
            (
              (.categories? // .categorie?)
              |= map(
                   .items |= map(
                     .body |= clean
                   )
                 )
            )
          ' news.json > news.cleaned.json
          mv news.cleaned.json news.json

      - name: Commit & push cleaned news.json
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: '🔄 Cleanup HTML/CSS residues from news.json'
          file_pattern: news.json
